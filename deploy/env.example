# ====================================
# Docker 生产环境配置示例
# ====================================

# 镜像配置
# 如果你已经构建好镜像并推送到 registry，可以直接指定镜像名
# 如果不指定，将使用默认值并通过 Dockerfile 构建
APP_IMAGE=bytedance-ai-agent:latest
IDP_IMAGE=bytedance-idp:latest

# ====================================
# Redis 配置
# ====================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_redis_password_here

# ====================================
# MongoDB 配置
# ====================================
MONGODB_URI=mongodb://mongo:27017/ai-agent

# 如果启用 MongoDB 认证，使用以下格式：
# MONGODB_URI=mongodb://username:password@mongo:27017/ai-agent

# MongoDB 初始化配置（可选）
# MONGO_INITDB_ROOT_USERNAME=admin
# MONGO_INITDB_ROOT_PASSWORD=your_secure_mongo_password

# ====================================
# Ollama AI 模型配置
# ====================================
OLLAMA_API_URL=http://ollama:11434
OLLAMA_MODEL=deepseek-r1:7b

# ====================================
# IDP (Identity Provider) 配置
# ====================================
IDP_PORT=9000
IDP_ISSUER=https://your-domain.com
IDP_CLIENT_ID=ai-agent-bff
IDP_REDIRECT_URIS=https://your-domain.com/api/auth/callback
IDP_POST_LOGOUT_REDIRECT_URI=https://your-domain.com/api/auth/logout/callback
# ⚠️ 重要：生产环境必须更改此密钥
IDP_CLIENT_SECRET=your_secure_idp_client_secret_here

# ====================================
# 应用配置
# ====================================
NODE_ENV=production
PORT=8080

# ====================================
# 第三方 API 配置
# ====================================

# Tavily 搜索 API
TAVILY_API_KEY=your_tavily_api_key_here

# Volcengine ARK API
ARK_API_KEY=your_ark_api_key_here
ARK_API_URL=https://ark.cn-beijing.volces.com/api/v3/chat/completions
ARK_MODEL=doubao-1-5-thinking-pro-250415

# ====================================
# OIDC 认证配置（可选）
# ====================================
# ⚠️ 重要：生产环境必须更改此密钥
OIDC_ISSUER=https://your-domain.com
OIDC_INTERNAL_ISSUER=http://idp-ai-agent:9000
OIDC_CLIENT_ID=ai-agent-bff
OIDC_CLIENT_SECRET=your_secure_oidc_client_secret_here
OIDC_REDIRECT_URI=https://your-domain.com/api/auth/callback
OIDC_POST_LOGOUT_REDIRECT_URI=https://your-domain.com/api/auth/logout/callback

# ====================================
# 日志配置（可选）
# ====================================
# LOG_LEVEL=info
# LOG_FORMAT=json

# ====================================
# 性能配置（可选）
# ====================================
# MAX_CONCURRENT_REQUESTS=10
# REQUEST_TIMEOUT=30000
# CACHE_TTL=3600
